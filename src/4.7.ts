/**

classnames

Напишите утилиту для объединения имён классов. 
Функция должна обрабатывать:
- объекты,
- строки,
- числа, но не 0,
- массивы любой вложенности.

---

Полученные классы удобно добавлять в массив. Затем преобразовывать его в строку методом join.
 
*/

function classNames() {
  // Код здесь
}

export { classNames };
export default classNames;
